// fetch data from JSON file

// const first_name =
// const last_name =

const url = `https://en.wikipedia.org/wiki/${first_name}_${last_name}`;

// find data inside the url

const photo = document.querySelector('.infobox.vcard img').src;

const bio = document.querySelectorAll('.mw-parser-output p')[1].innerText;
